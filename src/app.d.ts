declare global {
    namespace App {
        // interface Error {}
        interface Locals {
            theme: ThemeProps;
        }
        // interface PageData {}
        // interface PageState {}
        // interface Platform {}
    }
}

export {};
