import type { Size } from '$components/types';

export interface GlyphProps {
    size?: Size;
    disabled?: boolean;
    class?: string;
}

export interface IconProps {
    color?: string;
    size?: Size;
    disabled?: boolean;
    class?: string;
}
